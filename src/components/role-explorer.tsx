"use client";
import Link from "next/link";
import { useState } from "react";
const roles = [{ id: "tenant", title: "Arrendatario", icon: "⌂", color: "bg-blue", goal: "Encuentra un hogar y conversa directamente con el propietario.", steps: ["Explora el catálogo público", "Valida tu identidad", "Chatea y solicita contrato"], href: "/registro" }, { id: "landlord", title: "Arrendador", icon: "▣", color: "bg-orange", goal: "Publica un inmueble con respaldo municipal.", steps: ["Valida tu identidad", "Carga fotos y ubicación", "Responde consultas y formaliza"], href: "/registro" }, { id: "city", title: "Municipio", icon: "✦", color: "bg-violet", goal: "Da confianza verificando usuarios y publicaciones.", steps: ["Revisa documentos", "Aprueba propiedades", "Supervisa contratos"], href: "/login" }];
export function RoleExplorer() { const [selected, setSelected] = useState(roles[0]); return <div className="mt-10 grid gap-6 lg:grid-cols-[.9fr_1.1fr]"><div className="grid gap-3 sm:grid-cols-3 lg:grid-cols-1">{roles.map((role) => <button key={role.id} onClick={() => setSelected(role)} className={`rounded-2xl border p-5 text-left transition ${selected.id === role.id ? "border-blue bg-blue/5 shadow-lg" : "border-slate-200 bg-white hover:-translate-y-1"}`}><span className={`grid h-10 w-10 place-items-center rounded-xl ${role.color} text-xl text-white`}>{role.icon}</span><p className="mt-3 font-black text-navy">{role.title}</p><p className="mt-1 text-sm text-slate-500">Ver recorrido</p></button>)}</div><article className="rounded-3xl bg-navy p-7 text-white sm:p-9"><p className="text-sm font-bold uppercase tracking-[.18em] text-sky">Recorrido {selected.title}</p><h3 className="mt-3 text-3xl font-black">{selected.goal}</h3><ol className="mt-7 space-y-3">{selected.steps.map((step, index) => <li key={step} className="flex items-center gap-3 rounded-xl bg-white/10 p-3"><span className="grid h-7 w-7 place-items-center rounded-full bg-orange text-sm font-black">{index + 1}</span>{step}</li>)}</ol><Link href={selected.href} className="mt-7 inline-block rounded-xl bg-orange px-5 py-3 font-bold text-white">Comenzar este recorrido →</Link></article></div>; }
