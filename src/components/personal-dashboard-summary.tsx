"use client";
import { useEffect, useState } from "react";
type Data = { user: { fullName: string; email: string; phone?: string | null; nationalId?: string | null }; cards: { label: string; value: number }[] };
export function PersonalDashboardSummary() { const [data, setData] = useState<Data | null>(null); useEffect(() => { fetch("/api/my-dashboard").then(async (response) => response.ok ? response.json() : null).then(setData).catch(() => setData(null)); }, []); if (!data) return null; return <section className="rounded-2xl border border-sky/20 bg-gradient-to-r from-sky/10 to-white p-5"><div className="flex flex-wrap items-center justify-between gap-4"><div><p className="text-sm font-black text-navy">Tu espacio personal</p><p className="mt-1 text-sm text-slate-600">{data.user.email}{data.user.phone ? ` · ${data.user.phone}` : ""}</p></div><span className="rounded-full bg-white px-3 py-1 text-xs font-bold text-blue shadow-sm">Información privada</span></div><div className="mt-4 grid gap-3 sm:grid-cols-3">{data.cards.map((card) => <div key={card.label} className="rounded-xl bg-white p-4 shadow-sm"><p className="text-sm text-slate-500">{card.label}</p><p className="mt-1 text-2xl font-black text-navy">{card.value}</p></div>)}</div></section>; }
